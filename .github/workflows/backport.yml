name: Backport
on:
  pull_request:
    types:
      - closed
      - labeled

jobs:
  backport:
    runs-on: ubuntu-latest
    name: Backport
    steps:
      # - name: Backport
      - uses: sbrunner/backport-action@v2.0.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}