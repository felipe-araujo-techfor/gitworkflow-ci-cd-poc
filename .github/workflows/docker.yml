name: Container
on: push

jobs:
  build-container:
    runs-on: ubuntu-latest
    container:
      image: jdk-11.0.10_9_openj9-0.24.0-alpine
    steps:
      - name: Docker log
        run: |
          docker -v